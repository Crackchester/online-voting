extends layout

block content
    .text-center
        h1.display-4 You voted #{candidate.name} for the position of #{position}
        .row.pt-5
            .col-lg-3.offset-lg-3
                a(href=`/vote/${voter.uuid}/${position}`) Change Vote
            .col-lg-3
                a(href=`/vote/${voter.uuid}`) Return to Home